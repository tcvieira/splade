# @package _global_

data:
  Q_COLLECTION_PATH: [data/mmarco-pt/dev_queries]
  EVAL_QREL_PATH: [data/mmarco-pt/qrel/qrel.json]

config:
  top_k: 1000
  threshold: 0
  eval_metric: [[mrr_10, recall, ndcg, ndcg_cut, ndcg_rel]]
